<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- De-mfnr: standard Spring Boot parent replaces mfnr-parent. Boot 3.5.14 / SAS 1.5.7,
         matching the HelixIAM feature-branch line the two split services were already on. -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.14</version>
        <relativePath/>
    </parent>

    <groupId>io.helixiam</groupId>
    <artifactId>helix-iam-server</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <name>HelixIAM Server</name>
    <description>Standalone HelixIAM identity server (merged OAuth2/OIDC/SAML front + identity domain; no message broker).</description>

    <properties>
        <java.version>21</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!-- Security review L1: guava arrives transitively (OpenSAML / Shibboleth stack) at 31.1-jre,
             which is vulnerable to CVE-2023-2976 + CVE-2020-8908 (world-readable temp directories created
             by Files.createTempDir / FileBackedOutputStream). Both are fixed from 32.0.1-jre; 33.4.0-jre is
             the newest line that resolves in this offline (mvn -o) build with its own transitive metadata
             intact (failureaccess / listenablefuture). Pinned via the dependencyManagement block below. -->
        <guava.version>33.4.0-jre</guava.version>
        <!-- Security review M7: OpenSAML 4.3.2 is EOL (4.x line). The whole OpenSAML stack is forced to
             the current 5.1.4 line below. OpenSAML 5 replaces net.shibboleth.utilities:java-support:8.x with
             net.shibboleth:shib-support:9.1.4 (the *:shared package repackaging), bumps Apache Santuario
             xmlsec 2.3.x -> 3.0.5, and cryptacular 1.2.5 -> 1.2.6. All are pinned via dependencyManagement so
             no 4.3.2 / java-support:8 / xmlsec:2.3.4 leaks back onto the tree transitively (from
             spring-security-saml2-service-provider:6.5.10, which is compiled against OpenSAML 4 but is
             classpath-compatible with 5 and — verified — is NOT used by any HelixIAM code; OpenSAML is used
             directly). BouncyCastle stays uniformly at 1.81 (direct deps below, nearest-wins) so cryptacular's
             transitive bcprov:1.76 cannot reintroduce the split-version OperatorHelper NoClassDefFoundError. -->
        <opensaml.version>5.1.4</opensaml.version>
        <shibboleth.shared.version>9.1.4</shibboleth.shared.version>
        <xmlsec.version>3.0.5</xmlsec.version>
        <cryptacular.version>1.2.6</cryptacular.version>
    </properties>

    <!-- Transitive-dependency version overrides (security review). Boot's parent does not manage guava,
         so this dependencyManagement entry is what forces the patched line onto every consumer of it. -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.google.guava</groupId>
                <artifactId>guava</artifactId>
                <version>${guava.version}</version>
            </dependency>

            <!-- ===== M7: force the entire OpenSAML stack to 5.1.4 (from EOL 4.3.2) ===== -->
            <!-- opensaml-core (4.x) was split into opensaml-core-api + opensaml-core-impl in 5.x. -->
            <dependency>
                <groupId>org.opensaml</groupId>
                <artifactId>opensaml-core-api</artifactId>
                <version>${opensaml.version}</version>
            </dependency>
            <dependency>
                <groupId>org.opensaml</groupId>
                <artifactId>opensaml-core-impl</artifactId>
                <version>${opensaml.version}</version>
            </dependency>
            <dependency>
                <groupId>org.opensaml</groupId>
                <artifactId>opensaml-saml-api</artifactId>
                <version>${opensaml.version}</version>
            </dependency>
            <dependency>
                <groupId>org.opensaml</groupId>
                <artifactId>opensaml-saml-impl</artifactId>
                <version>${opensaml.version}</version>
            </dependency>
            <dependency>
                <groupId>org.opensaml</groupId>
                <artifactId>opensaml-security-api</artifactId>
                <version>${opensaml.version}</version>
            </dependency>
            <dependency>
                <groupId>org.opensaml</groupId>
                <artifactId>opensaml-security-impl</artifactId>
                <version>${opensaml.version}</version>
            </dependency>
            <dependency>
                <groupId>org.opensaml</groupId>
                <artifactId>opensaml-xmlsec-api</artifactId>
                <version>${opensaml.version}</version>
            </dependency>
            <dependency>
                <groupId>org.opensaml</groupId>
                <artifactId>opensaml-xmlsec-impl</artifactId>
                <version>${opensaml.version}</version>
            </dependency>
            <dependency>
                <groupId>org.opensaml</groupId>
                <artifactId>opensaml-messaging-api</artifactId>
                <version>${opensaml.version}</version>
            </dependency>
            <dependency>
                <groupId>org.opensaml</groupId>
                <artifactId>opensaml-messaging-impl</artifactId>
                <version>${opensaml.version}</version>
            </dependency>
            <dependency>
                <groupId>org.opensaml</groupId>
                <artifactId>opensaml-profile-api</artifactId>
                <version>${opensaml.version}</version>
            </dependency>
            <dependency>
                <groupId>org.opensaml</groupId>
                <artifactId>opensaml-profile-impl</artifactId>
                <version>${opensaml.version}</version>
            </dependency>
            <dependency>
                <groupId>org.opensaml</groupId>
                <artifactId>opensaml-soap-api</artifactId>
                <version>${opensaml.version}</version>
            </dependency>
            <dependency>
                <groupId>org.opensaml</groupId>
                <artifactId>opensaml-soap-impl</artifactId>
                <version>${opensaml.version}</version>
            </dependency>
            <dependency>
                <groupId>org.opensaml</groupId>
                <artifactId>opensaml-storage-api</artifactId>
                <version>${opensaml.version}</version>
            </dependency>
            <dependency>
                <groupId>org.opensaml</groupId>
                <artifactId>opensaml-storage-impl</artifactId>
                <version>${opensaml.version}</version>
            </dependency>

            <!-- OpenSAML 5's repackaged Shibboleth shared libs (java-support:8.x -> shib-*:9.1.4). -->
            <dependency>
                <groupId>net.shibboleth</groupId>
                <artifactId>shib-support</artifactId>
                <version>${shibboleth.shared.version}</version>
            </dependency>
            <dependency>
                <groupId>net.shibboleth</groupId>
                <artifactId>shib-security</artifactId>
                <version>${shibboleth.shared.version}</version>
            </dependency>
            <dependency>
                <groupId>net.shibboleth</groupId>
                <artifactId>shib-networking</artifactId>
                <version>${shibboleth.shared.version}</version>
            </dependency>
            <dependency>
                <groupId>net.shibboleth</groupId>
                <artifactId>shib-velocity</artifactId>
                <version>${shibboleth.shared.version}</version>
            </dependency>

            <!-- OpenSAML 5 crypto/XML-signature transitives: keep them current + convergent. -->
            <dependency>
                <groupId>org.apache.santuario</groupId>
                <artifactId>xmlsec</artifactId>
                <version>${xmlsec.version}</version>
            </dependency>
            <dependency>
                <groupId>org.cryptacular</groupId>
                <artifactId>cryptacular</artifactId>
                <version>${cryptacular.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- ============ Web / SAS / OIDC / SAML front (from publisher) ============ -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-authorization-server</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-thymeleaf</artifactId>
            <exclusions>
                <exclusion>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-starter-logging</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <!-- SAML2 SP (OpenSAML lives in the Shibboleth repo, declared below). Compiled against OpenSAML 4
             but classpath-compatible with the OpenSAML 5 line forced in dependencyManagement (M7); HelixIAM
             uses OpenSAML directly and references no org.springframework.security.saml2 class. The transitive
             OpenSAML 4 pulled net.shibboleth.utilities:java-support:8.x — excluded here so only the OpenSAML 5
             replacement net.shibboleth:shib-support:9.1.4 (added below) is on the tree. -->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-saml2-service-provider</artifactId>
            <version>7.1.1</version>
            <exclusions>
                <exclusion>
                    <groupId>net.shibboleth.utilities</groupId>
                    <artifactId>java-support</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <!-- M7: OpenSAML 5's repackaged shared utilities (net.shibboleth.shared.*), replacing java-support:8. -->
        <dependency>
            <groupId>net.shibboleth</groupId>
            <artifactId>shib-support</artifactId>
        </dependency>

        <!-- ============ Identity domain + persistence (from subscriber; de-mfnr'd) ============ -->
        <!-- Replaces mfnr-subscriber-starter-database-postgres: standard JPA + Postgres driver.
             The vendored io.helixiam.persistence.* (from starter-database) provides AttributeEncryption,
             FilterSpecification and optional read/write routing on top of this. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <!-- Versioned schema migrations (opt-in; unchanged from subscriber). -->
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
            <version>11.7.2</version>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
            <version>11.7.2</version>
        </dependency>

        <!-- ============ Session (KEEP Redis, per decision) ============ -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.session</groupId>
            <artifactId>spring-session-data-redis</artifactId>
        </dependency>

        <!-- ============ Crypto / MFA / passkeys ============ -->
        <dependency>
            <groupId>org.bouncycastle</groupId>
            <artifactId>bcprov-jdk18on</artifactId>
            <version>1.81</version>
        </dependency>
        <!-- Pin bcpkix/bcutil to match bcprov 1.81 — OpenSAML (via cryptacular) drags in 1.72,
             and the split versions collide (NoClassDefFoundError OperatorHelper) once both the
             SAML stack and the passkey/MFA stack share one classpath (strip-RabbitMQ merge). -->
        <dependency>
            <groupId>org.bouncycastle</groupId>
            <artifactId>bcpkix-jdk18on</artifactId>
            <version>1.81</version>
        </dependency>
        <dependency>
            <groupId>org.bouncycastle</groupId>
            <artifactId>bcutil-jdk18on</artifactId>
            <version>1.81</version>
        </dependency>
        <dependency>
            <groupId>com.j256.two-factor-auth</groupId>
            <artifactId>two-factor-auth</artifactId>
            <version>1.3</version>
        </dependency>
        <dependency>
            <groupId>com.google.zxing</groupId>
            <artifactId>javase</artifactId>
            <version>3.5.3</version>
        </dependency>
        <dependency>
            <groupId>com.webauthn4j</groupId>
            <artifactId>webauthn4j-core</artifactId>
            <version>0.21.5.RELEASE</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.dataformat</groupId>
            <artifactId>jackson-dataformat-cbor</artifactId>
            <version>2.18.1</version>
        </dependency>

        <!-- ============ Notifications (de-mfnr'd): direct SMTP, no broker ============
             Replaces mfnr-subscriber-starter-notification's AMQP emit. The vendored
             io.helixiam.notification.* keeps OTP code generation/storage; delivery goes through
             a pluggable Notifier (SMTP impl uses Jakarta Mail below; HTTP-webhook + log impls too). -->
        <dependency>
            <groupId>jakarta.mail</groupId>
            <artifactId>jakarta.mail-api</artifactId>
            <version>2.1.1</version>
        </dependency>
        <dependency>
            <groupId>org.eclipse.angus</groupId>
            <artifactId>angus-mail</artifactId>
            <version>2.0.3</version>
            <exclusions>
                <exclusion>
                    <groupId>jakarta.mail</groupId>
                    <artifactId>jakarta.mail-api</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- ============ Observability + API docs ============ -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.8.17</version>
        </dependency>

        <!-- ============ Config / misc ============ -->
        <dependency>
            <groupId>com.fasterxml.jackson.dataformat</groupId>
            <artifactId>jackson-dataformat-yaml</artifactId>
        </dependency>

        <!-- Logging: both source trees use Log4j2 (default logback excluded above/below). -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-log4j2</artifactId>
        </dependency>

        <!--
            Security review L4: spring-boot-devtools was REMOVED from the build. `<optional>true</optional>`
            keeps it out of *downstream consumers*, not out of this application's own compile/runtime
            classpath or its repackaged jar — devtools ships an auto-restart classloader and a LiveReload
            server, neither of which belongs anywhere near a production IdP. Nothing in this source tree
            references devtools APIs, so removing it does not affect compilation; a developer who wants
            live restart can re-add it in a personal profile.
        -->

        <!-- ============ Test ============ -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-starter-logging</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <!-- Embedded LDAP for the federation/LDAP-broker tests (from publisher). -->
        <dependency>
            <groupId>com.unboundid</groupId>
            <artifactId>unboundid-ldapsdk</artifactId>
            <version>7.0.5</version>
            <scope>test</scope>
        </dependency>
        <!-- Postgres integration tests for the merged app (new: replaces AMQP round-trip tests). -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <version>1.20.4</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>1.20.4</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

    <repositories>
        <!-- OpenSAML (org.opensaml:*) is not on Maven Central. -->
        <repository>
            <id>shibboleth-releases</id>
            <url>https://build.shibboleth.net/maven/releases/</url>
            <releases><enabled>true</enabled></releases>
            <snapshots><enabled>false</enabled></snapshots>
        </repository>
    </repositories>

</project>
